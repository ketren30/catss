
/* 
const initialState = {
    isLike: false,
    likeLink: nolike
}

export const cardReducer = (state = initialState, action) => {
    switch (action.type) {
        case "setLike":
            state.isLike= !state.isLike;
            if (state.isLike) state.likeLink=like;
            return state
        default: return state
    }
}  */